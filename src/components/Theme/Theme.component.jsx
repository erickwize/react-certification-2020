import React from 'react';
import { IconDark, IconContainer } from './Theme.styles';

const Theme = () => (
  <IconContainer>
    <IconDark />
    <p>Dark Mode</p>
  </IconContainer>
);

export default Theme;
