import styled from 'styled-components';

const Wrapper = styled.div`
  display: inline-block;
`;
const VideoList = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-evenly;
`;

export { Wrapper, VideoList };
